Before competition:
	- run the GetListFromFRC VI for the selected event (check year in URL and event code (code for colorado, AZFL for Arizona North))
	- update questions on the html form and update the line creating the QR code
	- rebuild and install application (or update the list of event in the main ini file and the team list ini file in the data folder
	- clear the c:\ProgramData\Scouting folder

Once schedule is published on Thursday night, run the GetSchedule VI after entering your team number and update(replace) the schedule.ini in the data folder.

